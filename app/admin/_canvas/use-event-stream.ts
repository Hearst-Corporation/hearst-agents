/** TODO stub — voir docs/AGENT-DRIVEN-DEV.md
 *
 * Hook EventSource live tail global. Stub no-op typé.
 * À implémenter : connexion SSE /api/admin/events, gestion mode live, push vers store.
 */

"use client";

/**
 * Active la connexion SSE quand `active === true`. Stub : ne fait rien.
 */
export function useEventStream(_active: boolean): void {
  /* TODO stub — no-op */
}

/** TODO stub — voir docs/AGENT-DRIVEN-DEV.md
 *
 * Canvas SVG du pipeline (nodes + edges). Stub composant placeholder.
 * À implémenter : viewport zoom/pan, rendu nodes, FlowEdge pour chaque edge, packets animés.
 */

"use client";

export default function FlowCanvas() {
  return (
    <div
      data-stub="FlowCanvas"
      className="flex-1 flex items-center justify-center text-text-faint t-13"
    >
      FlowCanvas (stub)
    </div>
  );
}

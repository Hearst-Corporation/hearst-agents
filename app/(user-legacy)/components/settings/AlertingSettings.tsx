/**
 * Re-export shim — la logique est désormais dans `./alerting/`.
 * API publique inchangée : `import { AlertingSettings } from "@/app/(user-legacy)/components/settings/AlertingSettings"`.
 */

export { AlertingSettings } from "./alerting/AlertingSettings";

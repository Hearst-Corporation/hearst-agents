/**
 * Re-export shim — préserve l'API publique historique
 * (`import { ContextRail } from "./ContextRail"`).
 *
 * Implémentation découpée sous `./context-rail/`.
 */

export { ContextRail } from "./context-rail/ContextRail";

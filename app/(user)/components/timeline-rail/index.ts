/**
 * timeline-rail/ — module du rail gauche multi-objet.
 *
 * Surface publique : `TimelineRail`. Les sous-composants restent
 * internes au module. Spec ADD : `docs/features/timeline-rail.md`.
 */

export { TimelineRail } from "./TimelineRail";

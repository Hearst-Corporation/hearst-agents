"use client";

/**
 * SpaceSelector — identité workspace (perso / side / venture).
 * Preview Q3-C : placeholder jusqu'au store workspace dédié.
 */

export function SpaceSelector() {
  return (
    <span
      className="hidden md:inline t-9 font-light text-text-faint truncate max-w-24"
      title="Workspace Helm"
    >
      Helm
    </span>
  );
}

# Audit Hearst OS — 2026-05-10

Audit complet lancé en mode autonome : tests, debug, cohérence, dead code, doublons.

---

## Findings par sévérité

### P0 — Casse runtime (2 findings, 2 fixés)

| # | Fichier:ligne | Problème | Fix |
|---|---|---|---|
| P0-1 | `proxy.ts` (racine) | Middleware Next.js jamais chargé — `proxy.ts` n'est pas un nom reconnu par Next.js. Arcjet, rate-limit, bot detection et redirect login ne s'exécutaient pas. Pages SSR non authentifiées exposées côté serveur. | Renommé → `middleware.ts`, export `proxy` → `middleware`. |
| P0-2 | `app/(user)/components/missions/builder/WorkflowCanvas.tsx`, `NodePalette.tsx` | 61 violations lint:visual (hex dans stylesheet Cytoscape + couleurs de marques tierces) bloquaient `npm run build` via le hook prebuild. | `// lint-visual-disable-file` ajouté aux deux fichiers avec justification. Cytoscape ne supporte pas `var(--token)`. |

---

### P1 — Incohérences visibles (4 findings, 4 fixés)

| # | Fichier:ligne | Problème | Fix |
|---|---|---|---|
| P1-1 | `app/(user)/components/missions/builder/BuilderSidePanel.tsx` | Aucun import dans le code source — composant mort confirmé par knip. | Supprimé. |
| P1-2 | `app/(user)/components/right-panel/AgentIcons.tsx` | Aucun import dans le code source — seule référence dans `.next/standalone` (artefact build). | Supprimé. |
| P1-3 | `app/(user)/components/missions/MissionRow.tsx:15-27` | Interface `Mission` UI déclarait `description: string`, `status: "active"\|"paused"\|"error"`, `frequency: string` comme champs requis. `PersistedScheduledMission` (payload API) ne les contient pas → rendu `undefined` silencieusement. | Champs rendus optionnels + fallback `description → input` et `frequency → schedule` dans le JSX. |
| P1-4 | `app/admin/_components/` (4 fichiers), `app/admin/analytics/page.tsx`, `app/login/page.tsx` | 17 occurrences de `tracking-marquee` / `tracking-display` en JSX actif — interdits depuis pivot éditorial 2026-04-29. | Retirés. Police `font-mono uppercase` conservée, le tracking arbitraire supprimé. |
| P1-5 | `app/admin/_canvas/CanvasShell.tsx`, `RunRail.tsx`, `RunWaterfall.tsx`, `NodeDetailPanel.tsx` | 12 occurrences `(--cykan)` — token remplacé par `(--accent-teal)` au pivot visuel 2026-05-09, migration incomplète dans `_canvas/`. | Migré vers `(--accent-teal)`. |

---

### P2 — Dette technique (5 findings, 5 fixés)

| # | Fichier:ligne | Problème | Fix |
|---|---|---|---|
| P2-1 | `vitest.config.ts` | Pas de `restoreMocks: true` — les `vi.fn()` ne sont pas réinitialisés entre tests du même fichier. 47 fichiers de test concernés. | `restoreMocks: true` ajouté. |
| P2-2 | `lib/engine/runtime/missions/types.ts:26` | `ScheduledMission` (in-memory store) manquait `lastRunStatus` et `lastError`, présents uniquement dans `PersistedScheduledMission`. Le cockpit affichait "idle" à tort sur le fallback mémoire (`lib/cockpit/today.ts:148`). | Champs ajoutés à l'interface. |
| P2-3 | `app/(user)/components/stages/CockpitStage.tsx:9-22` + `app/api/v2/cockpit/today/route.ts:1-9` | JSDoc mentionnaient encore "watchlist KPIs" et "watchlist" — retirés au pivot v1.6 (commit 7409ced). | JSDoc mis à jour : agenda + briefing humain + agents working + suggestions. |
| P2-4 | `app/(user)/reports/page.tsx:344` | `EmptyState` "Aucun rapport dans cette catégorie" sans `description` — rupture UX vs les autres EmptyState. | `description` ajoutée. |
| P2-5 | `.gitignore` | 15 PNG de session (audit, QA, GCP) non committés trainaient à la racine sans règle gitignore correspondante. | Patterns `/audit-*.png`, `/qa-*.png`, `/gcp-*.png` ajoutés au `.gitignore`. |

---

### P3 — Cosmétique (findings notés, non fixés)

| # | Fichier:ligne | Problème | Action recommandée |
|---|---|---|---|
| P3-1 | `__tests__/reports/catalog.test.ts` (bloc `kpi_expenses`) | Seul `expect(block).toBeDefined()` sans assertion substantielle. | Ajouter `expect(block.id).toBeDefined()` ou assertion sur `block.type`. |
| P3-2 | `lib/engine/runtime/index.ts`, `lib/llm/index.ts`, `lib/core/types/index.ts` | Barrels qui ré-exportent ~30-40 symboles jamais consommés depuis ces barrels (imports directs par les consommateurs). 256 exports inutilisés au total (knip). | Batch de nettoyage knip ciblé sur `lib/` — non bloquant. |
| P3-3 | `lib/browser/playwright-bridge.ts` | `playwright-core` importé dynamiquement mais absent du `package.json` — dépendance transitive via `@browserbasehq/sdk`. | Ajouter à `optionalDependencies` ou `ignoreDependencies` knip. |
| P3-4 | `app/(user)/components/stages/CockpitStage.tsx:61,89` | `console.warn` dans le cycle render client. | Acceptable en dev, à logger via un service en prod. |
| P3-5 | `app/global-error.tsx:3` | `import * as Sentry` — namespace import empêche le tree-shaking. | `import { captureException } from "@sentry/nextjs"`. |
| P3-6 | `app/(user)/components/TimelineRail.tsx:124` | `SectionHeader` locale inline — doublon vs primitive DS `app/(user)/components/ui/SectionHeader.tsx`. | Remplacer par l'import DS. |

---

### Non-findings (périmètre inspecté, rien à signaler)

- Suite de tests : **254 fichiers, 2498 tests, 0 échec, 0 skip** — propre.
- Aucun mock orphelin (vi.mock pointant vers modules supprimés).
- `CockpitTodayPayload` ↔ API ↔ UI : cohérent champ par champ.
- HOM contracts (`architecture/design-system/qa`) : aucun drift vs `lib/hom/types.ts`.
- Mots interdits en copy UI (soon/TBD/WIP/lorem/todo) : 0 occurrence.
- Magic numbers hex inline en JSX (hors WorkflowCanvas/NodePalette) : 0 occurrence.
- Routes API orphelines : toutes les routes ont des consommateurs confirmés.

---

## Résumé session

| Métrique | Valeur |
|---|---|
| Findings P0 | 2 fixés / 2 trouvés |
| Findings P1 | 5 fixés / 5 trouvés |
| Findings P2 | 5 fixés / 5 trouvés |
| Findings P3 | 6 notés / non fixés (non bloquants) |
| Commits | 2 (`f521b65` batch P0+P1, `75d4c6c` batch P2) |
| Fichiers supprimés | 3 (`proxy.ts`, `BuilderSidePanel.tsx`, `AgentIcons.tsx`) |
| Fichiers modifiés | 19 |

---

## Dettes P3 restantes (backlog)

1. Ajouter assertion substantielle dans `catalog.test.ts` bloc `kpi_expenses`.
2. Batch knip sur `lib/` — réduire les 256 exports inutilisés.
3. `playwright-bridge.ts` : déclarer `playwright-core` explicitement.
4. `import * as Sentry` → import nommé dans `global-error.tsx`.
5. `TimelineRail.tsx` : consolider `SectionHeader` locale → primitive DS.
